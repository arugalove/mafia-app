// Hardcoded game ID (for now)
const GAME_ID = 1;

const MESSAGE_CHAT = "chat message";
const MESSAGE_CONNECTED = "connection";

module.exports = {
    GAME_ID,
    MESSAGE_CHAT,
    MESSAGE_CONNECTED,
};
